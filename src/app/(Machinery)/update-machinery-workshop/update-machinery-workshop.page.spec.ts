import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateMachineryWorkshopPage } from './update-machinery-workshop.page';

describe('UpdateMachineryWorkshopPage', () => {
  let component: UpdateMachineryWorkshopPage;
  let fixture: ComponentFixture<UpdateMachineryWorkshopPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateMachineryWorkshopPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
