import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateMachineryDrivingSchoolPage } from './update-machinery-driving-school.page';

describe('UpdateMachineryDrivingSchoolPage', () => {
  let component: UpdateMachineryDrivingSchoolPage;
  let fixture: ComponentFixture<UpdateMachineryDrivingSchoolPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateMachineryDrivingSchoolPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
