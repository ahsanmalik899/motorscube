import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateMachineryHirePage } from './update-machinery-hire.page';

describe('UpdateMachineryHirePage', () => {
  let component: UpdateMachineryHirePage;
  let fixture: ComponentFixture<UpdateMachineryHirePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateMachineryHirePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
