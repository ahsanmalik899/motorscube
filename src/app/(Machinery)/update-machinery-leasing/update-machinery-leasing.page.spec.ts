import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateMachineryLeasingPage } from './update-machinery-leasing.page';

describe('UpdateMachineryLeasingPage', () => {
  let component: UpdateMachineryLeasingPage;
  let fixture: ComponentFixture<UpdateMachineryLeasingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateMachineryLeasingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
