import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateMachineryDealerPage } from './update-machinery-dealer.page';

describe('UpdateMachineryDealerPage', () => {
  let component: UpdateMachineryDealerPage;
  let fixture: ComponentFixture<UpdateMachineryDealerPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateMachineryDealerPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
