import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PostMachineryDrivingSchoolPage } from './post-machinery-driving-school.page';

describe('PostMachineryDrivingSchoolPage', () => {
  let component: PostMachineryDrivingSchoolPage;
  let fixture: ComponentFixture<PostMachineryDrivingSchoolPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PostMachineryDrivingSchoolPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
