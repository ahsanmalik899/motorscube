import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateMachineryShowroomPage } from './update-machinery-showroom.page';

describe('UpdateMachineryShowroomPage', () => {
  let component: UpdateMachineryShowroomPage;
  let fixture: ComponentFixture<UpdateMachineryShowroomPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateMachineryShowroomPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
