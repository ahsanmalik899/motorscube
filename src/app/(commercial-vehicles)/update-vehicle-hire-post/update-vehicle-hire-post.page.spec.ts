import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdateVehicleHirePostPage } from './update-vehicle-hire-post.page';

describe('UpdateVehicleHirePostPage', () => {
  let component: UpdateVehicleHirePostPage;
  let fixture: ComponentFixture<UpdateVehicleHirePostPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdateVehicleHirePostPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
