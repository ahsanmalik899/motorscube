import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdatePlantDealerPage } from './update-plant-dealer.page';

describe('UpdatePlantDealerPage', () => {
  let component: UpdatePlantDealerPage;
  let fixture: ComponentFixture<UpdatePlantDealerPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdatePlantDealerPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
