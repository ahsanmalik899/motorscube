import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UpdatePlantWorkshopPage } from './update-plant-workshop.page';

describe('UpdatePlantWorkshopPage', () => {
  let component: UpdatePlantWorkshopPage;
  let fixture: ComponentFixture<UpdatePlantWorkshopPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UpdatePlantWorkshopPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
